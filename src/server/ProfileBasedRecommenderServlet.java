package server;

import javax.servlet.http.HttpServlet;

/**
 * When the user sends a get request to /profile,
 * Displays the form where it lists first 15 movies and with
 * random ratings from 1 to 5, and asks the user to change them as needed.
 * When the user presses the Submit button, returns movie recommendations
 * for this user in the html table.
 */
public class ProfileBasedRecommenderServlet extends HttpServlet {

    // FILL IN CODE
}
